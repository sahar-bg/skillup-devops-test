import { ImageAltTextRule } from '../../src/rules/ImageAltTextRule';

describe('ImageAltTextRule Simple', () => {
  it('should exist', () => {
    const rule = new ImageAltTextRule();
    expect(rule).toBeDefined();
  });
});
